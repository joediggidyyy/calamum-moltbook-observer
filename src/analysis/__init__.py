"""Calamum Blind ML analysis tooling.

This package intentionally uses only the Python standard library unless and
until maintainer-approved third-party ML dependencies are introduced.

Design constraints:
- names-only (no raw Moltbook semantic payload)
- deterministic outputs where feasible (manifests + splits)
"""

from __future__ import annotations
