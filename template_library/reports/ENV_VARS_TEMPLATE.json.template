{
  "template_id": "VAULT_TEMPLATE_ENV_VARS_V1",
  "doc_type": "env_vars",
  "purpose": "Document required environment variables (names only, no values).",
  "links": {
    "policies": ["CREDENTIAL_MANAGEMENT_POLICY"],
    "related_docs": ["codesentinel.json.example"]
  },
  "variables": [
    {
      "name": "OPENAI_API_KEY",
      "required": false,
      "notes": "If using OpenAI integrations. Never commit values."
    },
    {
      "name": "GITHUB_TOKEN",
      "required": false,
      "notes": "For GitHub API access in automation."
    },
    {
      "name": "SENTRY_AUTH_TOKEN",
      "required": false,
      "notes": "For Sentry automation."
    },
    {
      "name": "EDGE_HOST",
      "required": false,
      "notes": "CIDS edge hostname (no hardcoded IPs)."
    },
    {
      "name": "EDGE_USER",
      "required": false,
      "notes": "CIDS edge user."
    },
    {
      "name": "EDGE_PASSWORD",
      "required": false,
      "notes": "CIDS edge password (do not store in repo)."
    },
    {
      "name": "BRAIN_USER",
      "required": false,
      "notes": "CIDS brain user."
    },
    {
      "name": "BRAIN_PASSWORD",
      "required": false,
      "notes": "CIDS brain password (do not store in repo)."
    }
  ]
}
